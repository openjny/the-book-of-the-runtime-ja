# CLR 入門

::: info 原文
この章の原文は [Introduction to the Common Language Runtime](https://github.com/dotnet/runtime/blob/main/docs/design/coreclr/botr/intro-to-clr.md) です。
:::

## 共通言語ランタイム (CLR) とは

共通言語ランタイム (Common Language Runtime, CLR) は、.NET プログラムの実行環境です。CLR は以下のような機能を提供します：

- **ガベージコレクション (GC)**: メモリの自動管理
- **JIT コンパイル**: 中間言語 (IL) からネイティブコードへの変換
- **型安全性**: 型の安全性を保証する仕組み
- **例外処理**: 構造化された例外処理の仕組み
- **スレッド管理**: マルチスレッドプログラミングのサポート

::: tip 💡 初心者向け補足
「ランタイム」という言葉は、「プログラムが実行される時」という意味です。CLR は、C# などで書いたプログラムが実際に動くときに必要な、裏方のソフトウェアです。Java でいう JVM (Java Virtual Machine) に相当します。
:::

## CLR の主要コンポーネント

CLR は複数のコンポーネントから構成されています：

### JIT コンパイラ (RyuJIT)

JIT (Just-In-Time) コンパイラは、プログラムの実行時に中間言語 (IL) をマシンが直接実行できるネイティブコードに変換します。

::: tip 💡 初心者向け補足
C# のコードは、まず「中間言語 (IL)」というものにコンパイルされます。この IL は、プログラムが実行されるときに JIT コンパイラによって、CPU が直接理解できる「ネイティブコード」に変換されます。これにより、異なる CPU アーキテクチャ (x86, ARM など) でも同じ IL コードを実行できます。
:::

### ガベージコレクタ (GC)

ガベージコレクタは、プログラムが使用しなくなったメモリを自動的に回収する仕組みです。詳しくは [ガベージコレクション](./garbage-collection) の章を参照してください。

### 型システム

CLR の型システムは、すべてのオブジェクトの型情報を管理します。詳しくは [型システム](./type-system) の章を参照してください。

---

> 📖 この章はまだ翻訳途中です。[翻訳に貢献する](https://github.com/openjny/the-book-of-the-runtime-ja)
